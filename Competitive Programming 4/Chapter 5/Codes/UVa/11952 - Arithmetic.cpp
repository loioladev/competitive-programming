#include <bits/stdc++.h>

using namespace std;

void control(){
    // get input
    string a1, b1, c1, tmp;
    cin >> a1 >> tmp >> b1 >> tmp >> c1;
    int a = stoi(a1);
    int b = stoi(b1);
    int c = stoi(c1);

    // 

}

int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin >> t;
    while(t--)
        control();
    return 0;
}
